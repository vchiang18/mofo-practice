//  import {configureStore} from '@reduxjs/toolkit';   // import configureStore from redux toolkit
//  import {stripeApi} from './api/stripe';   // import the API slice

//  export default configureStore({
//     reducer : {
//         [stripeApi.reducerPath]: stripeApi.reducer,   // add the reducer to the store
//     },
//     middleware: (getDefaultMiddleware) =>
//         getDefaultMiddleware().concat(stripeApi.middleware),   // add the middleware to the store
// });
